# Subnet projects have moved

All three subnet/AI-market projects that previously lived under this
directory have moved to a dedicated repo (2026-05-23) so the Python
curriculum repo stays scoped to its name:

  https://github.com/RondoAI/subnet-magazine

| Was here                              | Now lives at                  |
|---------------------------------------|-------------------------------|
| `projects/subnet-magazine/`           | `/v1/` in the magazine repo  |
| `projects/subnet-magazine-v2/`        | `/v2-articles/` in the repo  |
| `projects/subnet-terminal-v3/`        | `/v3-next/` in the repo      |

The live cockpit + 128-subnet curation (the cockpit version Claude has
been actively building) lives at the root of the magazine repo:

  https://raw.githack.com/RondoAI/subnet-magazine/main/cockpit.html

Existing commit-pinned URLs (any pre-split SHA on `rondo-AI-curriculum`)
keep working — git history preserves every file. Latest-branch URLs
now serve this pointer.

Per Rondo's instruction: Python education stays here, protected and
not mixed with subnet/market work.
